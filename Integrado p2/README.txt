Directorio que integra servidor/raspberry y el cliente

Para ejecutarse en una raspberry se deberá cambiar el valor de host de ambos .JSON
por el valor de la ip de la raspberry.

Ejecutar primero el berry/main desde la ruta /berry
Luego cliente/main desde la ruta /cliente

Berry tiene 2 modos de funcionamiento integrando camara Pi(V2) o camara web tradcional,
se puede cambiar entre funcionamiento comentando y descomentando en el main de berry
la linea que se desee que importe camara

*abrir consola en cada carpeta por separado*